export const viewport = {
  width: 'device-width',
  initialScale: 1,
};

export const metadata = {
  title: 'Election Timeline 2024 | VoteWise AI',
  description: 'Track the full schedule and milestones of the Indian general elections.',
};

export default function TimelineLayout({ children }) {
  return <>{children}</>;
}
