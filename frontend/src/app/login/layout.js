export const viewport = {
  width: 'device-width',
  initialScale: 1,
};

export const metadata = {
  title: 'Sign In | VoteWise AI',
  description: 'Access your personalized election dashboard and saved insights.',
};

export default function LoginLayout({ children }) {
  return <>{children}</>;
}
