export const viewport = {
  width: 'device-width',
  initialScale: 1,
};

export const metadata = {
  title: 'Election Glossary | VoteWise AI',
  description: 'Understand the key terms and concepts of Indian democracy.',
};

export default function GlossaryLayout({ children }) {
  return <>{children}</>;
}
