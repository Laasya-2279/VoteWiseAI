export const viewport = {
  width: 'device-width',
  initialScale: 1,
};

export const metadata = {
  title: 'Guided Voter Tour | VoteWise AI',
  description: 'Step-by-step instructions on how to register and vote in India.',
};

export default function GuideLayout({ children }) {
  return <>{children}</>;
}
