export const viewport = {
  width: 'device-width',
  initialScale: 1,
};

export const metadata = {
  title: 'Interactive Election Phase Map | VoteWise AI',
  description: 'Explore the 7-phase Indian general election schedule with our interactive thematic map.',
};

export default function PhaseMapLayout({ children }) {
  return <>{children}</>;
}
